export type PostType = {
    id: number,
    userId: number,
    title: string,
    body: string
}

export type CommentType = {
    id: number,
    email: string,
    body: string
}

export type AuthorType = {
    name: string
  }